import 'start.dart';

void main(List<String> arguments) {
  // create obj from StartApp to start the application and call method libraryBookMenu
  StartApp start = StartApp();
  start.libraryBookMenu();
}
